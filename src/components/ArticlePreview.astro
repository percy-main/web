---
import { RichText } from "@/components/RichText";

type Props = {
  id: string;
  title: string;
  when: string;
  summary: string;
};

const { id, title, when, summary } = Astro.props;
---

<div class="bg-white rounded-lg shadow-md border p-4 m-2flex items-center mb-4">
  <div>
    <div>
      <h2 class="text-lg font-semibold">{title}</h2>
      <p class="text-gray-500 text-sm">{when}</p>
    </div>
  </div>
  <p class="text-gray-700 mb-4">
    <RichText document={summary} />
  </p>
  <div class="flex justify-end">
    <a href={`/news/article/${id}`} class="text-blue-500 font-semibold">
      Read More
    </a>
  </div>
</div>
